import Endpoint from '../../components/endpoint'

# Authentication
### Introduction
Authentication is used to create, validate, and log users into the app

#### Endpoints
{/* TODO da eliminare l'esempio qui sotto */}
<Endpoint
   method="POST"
   path="/auth/register"
   reqType="RegisterRequest"
   resType="RegisterResponse"
   contentType="application/json"
   params={
      [
         {
            name: "Esempio",
            description: "es"
         }
      ]
      [
         {
            name: "Esempio2",
            description: "es"
         }
      ]
   }
/>
