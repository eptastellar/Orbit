import Endpoint from '../../components/endpoint'

# Authentication
### Introduction
Authentication is used to create, validate, and log users into the app

#### Endpoints
{/* TODO da eliminare l'esempio qui sotto */}
<Endpoint
   method="GET"
   path="/auth/log-out/"
   reqType=""
   resType="SuccessResponse"
   contentType="application/json"
/>
<Endpoint
   method="GET"
   path="/auth/sign-in/"
   reqType="AuthRequest"
   resType="AuthResponse"
   contentType="application/json"
/>
<Endpoint
   method="POST"
   path="/auth/sign-up/"
   reqType="SignUpValidateRequest"
   resType="AuthResponse"
   contentType="application/json"
/>
