export const resolveServerError = (error: string): string => {
   // let resolvedError = ""

   // if (error.includes("validation/username-too-long"))
   //    resolvedError = "Email address already in use."
   // else if (error.includes("auth/invalid-credential"))
   //    resolvedError = "Provided invalid credentials."
   // else resolvedError = error

   // return resolvedError
   return error
}
